name: Apply asteroid fuel i18n cleanup

on:
  push:
    branches:
      - fix/asteroid-fuel-preview-layout
    paths:
      - scripts/_apply_asteroid_fuel_i18n_fix.py
      - .github/workflows/apply-asteroid-fuel-i18n-fix.yml

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: fix/asteroid-fuel-preview-layout
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-prod.txt pytest

      - name: Apply i18n cleanup
        run: python scripts/_apply_asteroid_fuel_i18n_fix.py

      - name: Remove one-shot tooling
        run: |
          git rm scripts/_apply_asteroid_fuel_i18n_fix.py
          git rm .github/workflows/apply-asteroid-fuel-i18n-fix.yml

      - name: Verify
        env:
          APP_ENV: development
          GC_SKIP_MIGRATION_CHECK: "1"
          SECRET_KEY: ci-asteroid-fuel-i18n-secret-key
        run: |
          node --check static/js/galaxy-quick-action.js
          python -m pytest -q --tb=line tests/test_asteroid_value_preview.py tests/test_asteroids.py
          python scripts/audit_visible_i18n.py --raw-only --diff-base origin/main --max-findings 100

      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: make asteroid fuel preview i18n-safe"
          git push origin HEAD:fix/asteroid-fuel-preview-layout
