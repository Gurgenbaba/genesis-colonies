name: TEMP Apply PG Imperial Directives Single Conn

on:
  push:
    branches:
      - perf/pg-imperial-directives-single-conn

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: perf/pg-imperial-directives-single-conn
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install scoped test dependencies
        run: python -m pip install -r requirements-prod.txt pytest
      - name: Apply exact Imperial Directives patch
        run: python scripts/_apply_pg_imperial_directives_single_conn.py
      - name: Verify source boundary
        run: |
          python -m py_compile app.py game/directives/generator.py tests/test_gc_pg_imperial_directives_single_conn.py
          python -m pytest -q tests/test_gc_pg_imperial_directives_single_conn.py tests/test_imperial_directives_page.py tests/test_imperial_directives_service.py
      - name: Commit generated product diff
        run: |
          if git diff --quiet -- app.py game/directives/generator.py; then
            echo "Imperial Directives patch already applied."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py game/directives/generator.py
          git commit -m "perf(pg): reuse one connection for Imperial Directives SSR"
          git push origin HEAD:perf/pg-imperial-directives-single-conn
