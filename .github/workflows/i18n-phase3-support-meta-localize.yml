name: I18N Phase 3 Support Meta Localize

on:
  push:
    branches:
      - fix/i18n-phase3-support-worldboss
    paths:
      - scripts/_gc_i18n_phase3_support_meta_localize.py
      - .github/workflows/i18n-phase3-support-meta-localize.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply localized support message meta formats
        run: python scripts/_gc_i18n_phase3_support_meta_localize.py
      - name: Commit generated locale changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add locales
          if git diff --cached --quiet; then
            echo "No generated changes."
            exit 0
          fi
          git commit -m "fix(i18n): localize support message meta format"
          git push
