name: World Boss Live Payload Performance Gate

on:
  pull_request:
    paths:
      - "app.py"
      - "game/world_boss.py"
      - "static/main.js"
      - "tests/test_gc_perf_world_boss_live_payload_001.py"
      - ".github/workflows/world-boss-live-perf.yml"
  push:
    branches: [main]
    paths:
      - "app.py"
      - "game/world_boss.py"
      - "static/main.js"
      - "tests/test_gc_perf_world_boss_live_payload_001.py"
      - ".github/workflows/world-boss-live-perf.yml"

jobs:
  world-boss-live-perf:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    env:
      APP_ENV: development
      FLASK_ENV: development
      FLASK_DEBUG: "0"
      SECRET_KEY: ci-world-boss-live-not-production
      GC_SKIP_MIGRATION_CHECK: "1"
      GC_EMBEDDED_CRON: "0"
      GC_DB_BACKEND: sqlite
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-prod.txt pytest
      - name: Compile and diff gate
        run: |
          python -m py_compile app.py game/world_boss.py tests/test_gc_perf_world_boss_live_payload_001.py
          git diff --check
      - name: Compact live payload regression
        run: |
          python -m pytest -q --tb=line \
            tests/test_gc_perf_world_boss_live_payload_001.py \
            tests/test_gc_pg_world_boss_readonly_payload.py \
            tests/test_world_boss.py
