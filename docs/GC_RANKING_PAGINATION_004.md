# GC-RANKING-PAGE-004

Ranking keeps exact arbitrary-precision ordering, exposes the complete ordered player/alliance dataset to the ranking page, and shows at most 100 entries per visible page. Page controls are client-side so category tabs keep their existing full-universe ordering semantics.

- visible page size: 100
- desktop + mobile pager
- no score formula or rank semantics changed
- internal bounded ranking callers keep their requested limits
